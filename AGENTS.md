# Octav Learning — Agent Guide

## Session Workflow (required)

**At the start of every work session:** read the top 2–3 entries of `docs/PROGRESS.md` to learn what was done recently and what the agreed next steps are. Do not re-run a full project-wide analysis unless the log is stale or contradicts what you see.

**At the end of every work session** (after enhancements, bug fixes, or content additions): prepend one entry to `docs/PROGRESS.md` (newest at top), using this format:

```
## YYYY-MM-DD — <short title>
Git HEAD: `<short-hash>` (branch, tree clean/dirty)
Done: <what changed — files, topic IDs, features; be specific>
Verified: <which checks ran and passed: validate:content, audit:content, npm test, e2e…>
Next: <updated next steps — remove completed items, add new ones discovered>
Notes: <anything a future session must know: blockers, decisions, conventions changed>
```

Rules:
- Keep entries short (under ~15 lines). Facts, not prose.
- Always record verification results honestly — if a check failed or wasn't run, say so.
- Keep the "Next" list current; it is the queue for the next session.
- If you change a workflow, convention, or command, update this `AGENTS.md` too.

## Quality Gates (run before considering work done)

```bash
npm run generate:registry      # after adding/removing topic JSON files
npm run validate:content
npm run validate:illustrations
npm run validate:illustration-layout
npm run audit:content          # fails on any warning
npm test                       # Vitest unit tests
npm run test:e2e               # Playwright (auto-starts dev server)
npm run test:e2e:static        # static-export build + full suite against out/ (pre-deploy gate)
```

## Conventions

- Product name is **"Octav Learning"** (formerly IBLearn); the `iblearn_progress` localStorage key and `iblearn-*` AWS resource names are intentionally unchanged. PWA icon source is `public/icons/icon.svg` (full-bleed variant of the Octav mark), regenerated with `node scripts/generate-icons.mjs`.
- Content lives in `src/content/data/topics/<subject>/<topic-id>.json` — one file per topic, plus a per-subject `order.json` (JSON array of every topic id in curated pedagogical order; the registry emits topics in that sequence — subject pages, diagnostics, mixed-review). Strict: a new topic without an `order.json` entry fails `generate:registry` and `validate:content`. See `docs/CONTENT_STYLE.md`.
- 10 subjects (added 2026-08-19): math, english, biology, chemistry, physics, geography, history, ict, chinese, german. Subject list/order/colours: `src/content/data/subjects.json`; new subjects also need the `SubjectId` union (`src/content/types.ts`), the zod enum (`src/content/schema.ts`), an emoji in `src/lib/subject-emoji.ts` (single map — used by homepage, /progress, subject pages), a course entry in `src/lib/courses.ts` (for diagnostics/exams/ladder), and count/list updates in `tests/unit/content-registry.test.ts`, `tests/unit/content-schema.test.ts`, `tests/e2e/app.spec.ts`, `tests/e2e/diagnostics.spec.ts`, `tests/e2e/exams.spec.ts`. Language subjects (chinese/german) use vocab-table notes + bilingual flashcards; ICT/content outside math must avoid literal `$` (InlineMath) — reword or use fullwidth ＄. Currency amounts in math content must ALSO use fullwidth ＄ as plain text (never KaTeX `\$` inside `$...$` — the InlineMath splitter mis-pairs the delimiters and garbles the text; `audit:content`'s `escaped_dollar` rule enforces this).
- Free-response practice sets live in `src/content/data/papers/<courseId>/<courseId>-set-<n>.json` — original questions only, 20 marks per set, `marks === markscheme.length`. See `docs/CONTENT_STYLE.md` ("Practice papers").
- Course groupings (diagnostics/exams/ladder/papers) come from `src/lib/courses.ts` — add new courses there.
- **External dependencies get a controllable dummy** (user directive): unit tests mock them; e2e/local dev run a dummy implementation with deterministic defaults + per-test response injection. The injection path doubles as production-issue reproduction. See `src/lib/feedback/dummy.ts` (the template) and `docs/ai-feedback.md`.
- Illustration rules: `docs/ILLUSTRATION_GUIDELINES.md`. SVGs go in `public/images/<subject>/`. Visual review: `npm run render:illustrations` renders every SVG to `illustration-previews/<subject>/*.png` + an `index.html` contact sheet (gitignored; `--subject=<name>` or a single SVG path as arg). Use it for the manual colour/accuracy pass the validators can't do — an agent can sweep the PNGs with image reading.
- UX rules: `docs/UX_GUIDELINES.md` (tokens, chrome asymmetry, a11y checklist, copy voice). **Standing UX-review pass:** any change to user-visible surfaces (`src/app/**` pages, `src/components/**` chrome) gets a UX-review subagent pass before the work counts as done — Playwright screenshots of every changed surface in mobile 375px + desktop, light + dark (plus state variants), reviewed by a fresh-context subagent against the guidelines; fix or consciously waive each finding and record waivers in the PROGRESS.md entry. Same screenshots-as-artefact pattern as render:illustrations; keep screenshots out of the repo.
- `src/content/registry.ts` is generated — never edit by hand; use `npm run generate:registry` (re-run after adding/removing topic OR paper JSON files).
- SEO (plan: `docs/seo-technical-plan.md`): curriculum tiers (ks3/igcse/ibdp), hub/leaf paths, `curriculumLabel`, `courseCodeFor` and the hreflang phase switch live in `src/lib/seo/{curriculum,site,hreflang}.ts` — import them, never re-derive a tier from a topic id. `npm run generate:sitemaps` writes `public/sitemap/*.xml` (gitignored, generated; `lastmod` comes from `git log`, because content files have no date field); `--check` = staleness gate, `--verify` = run AFTER `build:static` and fail on any sitemap URL that is not a prerendered page in `out/`. Indexable set is deliberately ~310 of ~800 URLs: `…/quiz` and `…/flashcards` are `noindex, follow`, as are premium surfaces (`/exams/<c>/paper-N`, paper sets 2+, ladder levels 3–5). `--verify` (run AFTER `build:static`) is the real gate: it fails on any sitemap URL that 404s, any sitemap URL the page itself marks noindex, any indexable page in no sitemap, and any duplicate `<title>` among indexable pages. Do not advertise the sitemap in `robots.ts` until the `/ks3` + `/ibdp` hub routes exist (that's what `--verify` currently reports as 13 unbuilt hubs). **Titles:** `src/lib/seo/page-meta.ts`'s `pageMeta()` is the ONLY sanctioned title builder — it returns the brand-free half and lets the root `title.template` append " · Octav Learning", so a title string that repeats the brand renders it twice (the old /pricing defect). Topic leaves are deliberately brand-free (`title: { absolute }`); tools are `noindex, follow` + canonical to `/study`; premium surfaces must be noindex and are derived from `isFreePaperSet`/`isFreeLadderLevel`, never hardcoded. `'use client'` pages can't export metadata — their noindex lives in a passthrough segment `layout.tsx` (`/account`, `/progress`, `/admin`). Metadata strings must pass `plainText()` (KaTeX → glyphs) and the display-width budget (CJK = 2 cells); `subjectSeoName()` says "Maths" in metadata only.
- New topics follow the 7 notes / 12 flashcards / 15 questions standard; every question (MC and free-response) needs a `difficulty` tag — rubric in `docs/CONTENT_STYLE.md`.
- Topic taxonomy: `stage` (ks3/igcse/dp) + optional `year`/`course`/`level`/`strand` (strand = KS3 English only: reading/writing/grammar-vocabulary/spoken-english, grouped on the subject page) — see `docs/CONTENT_STYLE.md` ("Stage & course tagging" and ID conventions). `ibLevel` was retired in the Phase 1 migration (2026-07).
- Roadmap: `docs/revised-implementation-plan.md` (phases) and `docs/phase-1-implementation-plan.md`. Gating/monetization: `docs/entitlement-policy.md` (agreed 2026-08-22 tier split — anonymous funnel / free-with-login incl. 30 free AI marks per calendar month per account / premium unlimited AI + full exam tier) + `docs/entitlement-implementation-plan.md` (Phase E0–E4).
- Entitlements (Phase E, E0–E3 done 2026-08-22; E4 Stripe deferred): `user.tier` (`free` default on registration, `withTierDefault` fails closed — manual DDB grant until E4) + the static `TIER_FEATURES` map in `src/lib/entitlements/features.ts` are the single source of truth (server derives the `me()` `entitlements` list from it). Client gating = `EntitlementsContext.has(featureId)` + `LockedFeature` (UX-only, visible-but-inert preview + `/pricing` link; `compact` mode for repeated per-course rows; renders unlocked while entitlements load — never gate server APIs on the client context). Exam/ladder free-vs-premium split lives in `src/lib/entitlements/exam-access.ts` (first paper set per course + ladder levels 1–2 free; mock mode + sets 2+ + levels 3–5 premium tease). E4 must replace `AuthContext.login`'s client-side tier derivation with server-provided entitlements when the §2.9 Feature/Entitlement tables land.
- BBC reference pipeline (Phase 1.5): `tools/scripts/scrape-bbc-ks3.mjs` scrapes to `tools/data/`; `tools/scripts/convert-bbc-to-topics.mjs` (map: `tools/scripts/bbc-curation-map.json`) writes reference drafts to `tools/data/_staging/`. BBC text is **reference only** — notes are rewritten in our own voice, flashcards/questions authored, before anything lands in `src/content/data/topics/`. Out-of-scope subjects are archived in `tools/data/_archive/`.
- Deploy: **two live environments since 2026-08-11** (custom-domain-cutover-plan.md): **PROD = https://octavlearning.com** (www → apex 301; bucket `iblearn-prod-site-305655353474`, distribution `EYC8IOH4L9SXT`; deploys from `main`) and **DEV = https://dev.octavlearning.com** (since 2026-08-13 — dedicated ACM cert, separate from the prod cert so SAN changes never pull PROD into the change cone; the cloudfront.net URL `https://d2c1g77zfmjpm3.cloudfront.net` still works alongside; bucket `iblearn-site-305655353474`, distribution `E1BMVEW6YOKNUY`; deploys from `develop`). Both proxy `/api/feedback` to the same feedback **Lambda** (Session 3), **live since 2026-08-09 with DeepSeek** (`deepseek-v4-flash`; Moonshot abandoned — `api.moonshot.cn` gets blackholed from AWS ap-east-1); Function URL CORS allows dev + dev.octavlearning.com + apex + www origins. Unset `FEEDBACK_ENV` = unconfigured mode: GET `{configured:false}`, POST 501, "Mark with AI" hidden. Both also route `/api/auth/*` to the accounts **auth Lambda** (Phase B — email-OTP; more specific CloudFront behavior, listed before `/api/*`). **Email delivery — provider-swapped to Resend (2026-08-21):** SES production access was denied twice (sandbox, ap-southeast-1), so the `EmailSender` seam now selects the provider via the `EMAIL_PROVIDER` env var `{"NAME":"resend|ses|dummy","API_KEY":"..."}` (precedence over `AUTH_EMAIL`; `src/lib/auth/deps.ts`). The SES sandbox + terraform `ses` module are kept dormant — **no further SES appeals** (denied 3×; user decision 2026-08-25: revisit only if the account gains substantial AWS usage). Plan: `docs/aws-deployment-plan.md`; provider notes: `docs/ai-feedback.md`.
- CI/CD (Session 4, merged 2026-08): single workflow `.github/workflows/ci.yml` — `build-and-test` (validate/audit/lint/vitest/build) → `e2e` (matrix: iPhone SE / iPad Pro / Desktop Chrome, `fail-fast: false`, per-device report artifacts on failure) → deploy. `illustrations` (validate:illustrations + validate:illustration-layout, needs Playwright Chromium) and the security scans run as separate jobs in parallel with `e2e` — all four are in deploy's `needs`, keeping gates green-only without duplicating work in the deploy job. e2e IS a deploy gate since the matrix split brought it under ~10 min. Two deploy jobs (custom-domain-cutover-plan.md §4), one terraform state, shared `deploy` concurrency group: **`deploy-dev`** on push/dispatch to `develop` (cloudfront.net URL), **`deploy-prod`** on push/dispatch to `main` (octavlearning.com; extra www→apex 301 smoke check); each: build:static (now `generate:sitemaps && build-static.sh && verify:sitemaps` — the sitemap ships with every export and the 404/noindex/orphan gate runs in CI) → build:lambda → terraform apply → s3 sync → invalidation → smoke (smoke now also asserts `/api/auth/me` 401s and a `POST /api/auth/request-otp` probe returns **strict 200/429** (tightened 2026-08-28 — 502 no longer accepted) — catching missing table/index IAM AND provider rejection; 429 means the durable per-email budget was consumed by an earlier probe and still proves the DynamoDB path; the probe address is Resend's magic `delivered@resend.dev` (example.com is 422-rejected by Resend)) → **live SEO verification** (`verify:seo:live --all`; the dev job adds `--env=dev --origin=https://dev.octavlearning.com`) — the origin-facing gate that proves the sitemaps actually shipped, every submitted URL is live + indexable, the noindex contract holds at the edge, and the origin is serving THIS build (the local `verify:sitemaps` only proves the build; this caught the 2026-09-02 gap where `public/sitemap/` never reached S3). Auth via GitHub OIDC → role `arn:aws:iam::305655353474:role/iblearn-github-deploy` (`terraform/modules/ci`; trust scoped to repo `yongouyang/ib-learning-site` @ `develop` + `main`, no stored keys). Requires repo **variable** `AWS_DEPLOY_ROLE_ARN` (Settings → Secrets and variables → Actions → Variables) and optional **secret** `FEEDBACK_ENV` (JSON map, e.g. `{"FEEDBACK_PROVIDER":"openai-compatible","FEEDBACK_API_KEY":"sk-..."}` — unset = unconfigured Lambda). Optional **secret** `AUTH_ENV` overrides the auth Lambda env (unset = base dynamodb/ses wiring from envs/prod). Optional **secret** `EMAIL_PROVIDER` (`{"NAME":"resend","API_KEY":"re_..."}`) selects the email provider (unset = base `AUTH_EMAIL=ses`). Manual dispatch has a `deploy_only` input (escape hatch: skips all gate jobs, deploys the selected branch's environment as-is — the branch picked at dispatch time selects the env, no env picker; for deploy-stage fixes without a full-pipeline wait; deploy jobs use `always() && !failure()` so skipped gates don't block them but real failures do). The `FEEDBACK_ENV` / `AUTH_ENV` / `EMAIL_PROVIDER` JSON MUST be single-line with straight quotes — terraform parses them as HCL and rejects multi-line values / curly quotes.
- Security scanning (2026-08, plan `docs/security-scanning-plan.md`, Phase 3 done): Semgrep (SAST, pipx CLI, `p/typescript`+`p/react`+`p/security-audit`+`p/secrets` packs, excludes in `.semgrepignore`) + OSV-Scanner (SCA, official reusable workflow, recursive — covers `package-lock.json` AND `tools/package-lock.json`, ignores in `osv-scanner.toml`, results in Security → Code scanning) run as `ci.yml` jobs — full gates for PRs AND in deploy's `needs`. `.github/workflows/security.yml` is nightly-only (03:17 UTC CVE watch). Local triage: `npm run security` (needs `brew install semgrep osv-scanner`). No active ignores — the 5 next@15.5.x nested-pin CVE ignores were cleared by the Next.js 16 upgrade (now next@16.3.0; `osv-scanner.toml` comment records this).
- Terraform: `terraform/bootstrap` (one-time, LOCAL state by design: state bucket `iblearn-tfstate-305655353474` + lock table `iblearn-tfstate-lock`, ap-east-1), `terraform/envs/prod` (remote state), `terraform/modules/site` (bucket + OAC + CloudFront + URL-rewrite Function + `/api/*` behavior; the dev instance also sets `dev_brand_rewrite = true`, which makes the rewrite Function serve the DEV-badged `manifest-dev.webmanifest` / `*-dev` icon variants at the edge — `DevEnvironmentIndicator` only renders the red rim + DEV label, it must NEVER mutate metadata-managed head links: Next head reconciliation re-inserts the tag and you get two `link[rel=manifest]`, the pwa.spec.ts failure fixed 2026-08-28), `terraform/modules/feedback_api` (Lambda + Function URL + IAM + logs), `terraform/modules/auth_api` (accounts Lambda + Function URL + least-privilege IAM incl. GSI `index/*` ARNs; `reserved_concurrent_executions` is a variable defaulting to null — the account's ap-east-1 concurrent-executions quota is 10 (L-B99A9384), so ANY reservation fails PutFunctionConcurrency; re-enable only after a Service Quotas increase), `terraform/modules/progress_api` (progress Lambda + Function URL + IAM on octav-progress incl. `index/*` + users/sessions session-validation grants + `UpdateItem` on octav-leaderboard for the D4 award hook — the single XP writer), `terraform/modules/leaderboard_api` (Phase D board Lambda + Function URL — READ-ONLY IAM: Query/GetItem on octav-leaderboard + users/sessions session-validation grants; erasure lives on the auth Lambda: Query on the user-index GSI + DeleteItem), `terraform/modules/dynamodb` (Phase 0 tables + `octav-rate-limits` + `octav-leaderboard` + `octav-contact`), `terraform/modules/analytics_api` + `terraform/modules/admin_api` + `terraform/modules/analytics_report` (EventBridge 7pm HKT daily report, no Function URL) + `terraform/modules/contact_api` (Feature 3), `terraform/modules/ci` (GitHub OIDC provider + deploy role). AWS provider pinned `~> 6.0` (needed for the Lambda's `nodejs24.x` runtime — 5.x's enum ended at nodejs22.x). All local AWS/Terraform commands run with `AWS_PROFILE=ib-learning-site` (IAM admin user, account 305655353474). **No local `terraform apply` (user directive, 2026-08-11 incident): applies are CI-only** — local runs lack `FEEDBACK_ENV` (it lives only in the GitHub secret) and a local apply silently resets the Lambda to unconfigured mode, breaking "Mark with AI" in prod. Even a `-target` apply is unsafe: targeted resources' *dependencies* get applied too, which is how the Lambda got wiped. Exception: once the DeepSeek key moves to SSM Parameter Store (standing backlog item), local applies with the key sourced from SSM are fine again. Local `plan`/`validate`/`fmt` remain OK.

- Progress handler contract lives in `src/lib/progress/http-handler.ts` — the Next routes under `src/app/api/progress/*` (dev/e2e) AND `lambda/progress/index.ts` (prod, behind the CloudFront `/api/progress/*` behavior, listed before `/api/*`) both delegate there. Phase C conventions: per-profile progress namespaces (SKs `META#<profileId>`, `TOPIC#<profileId>#<subject>:<topic>#<attemptId>`, `EXAM#...`, `LADDER#...`, `FLASHCARD#...`; client localStorage key `octav_progress:<userId>:<profileId>`, anonymous `iblearn_progress` unchanged for logged-out); conflict rules = attempts/exams union by attemptId, ladder per-level max, flashcards LWW by lastReviewed, META per-field max (per-field conditional updates — a single whole-record SET would regress unimproved fields); every server write is ONE conditional DynamoDB command (atomic + idempotent — replays are no-ops); identity always from the session cookie via the SHARED `src/lib/auth/session.ts` `resolveSession` (profileId in payloads is data, validated against the session user's childProfiles); budgets 100 events/sync, 200 questionResults/attempt, 50-attempt read cap per topic; hardening: client dates REJECTED beyond now+24h (400 — both event dates and clientMeta.lastStudyDate; a >24h-fast device clock poisons those events' stored dates, so their re-uploads 400 forever — see PROGRESS.md known issues), ids `/^[A-Za-z0-9_-]+$/` (also on the AUTH accountUpdateSchema profileId — a stored bad id makes a profile permanently unsyncable), totalStars ≤ 1e6; `GET /api/progress/_health` is the unauthenticated CI smoke probe (fixed probe key, Limit-1 Query — 200 only). Session-validation storage for the progress Lambda is `DynamoSessionStorage` (users+sessions only — it needs NO otp/rate-limit table names); `listProgressByUser` LOOPS LastEvaluatedKey in BOTH adapters — progress AND the auth adapter (export/delete-account) never truncate. The dummy universe (`InMemoryProgressStorage`) is SHARED with the auth deps (auth routes and progress routes see one state in dev/e2e). Client sync (`src/lib/sync-manager.ts`) sends the queue in ≤100-event CHUNKS: 400 drops that chunk (never retried), transient failures stop mid-sequence leaving the rest queued, 401 purges; queue removals use the FLUSH-CAPTURED identity (not the live one); merge-on-login re-uploads id-bearing local-only attempts/exams (the self-healing path). Anonymous-data migration is at-most-once PER DEVICE (anon blob cleared + `octav_anon_claimed` flag) and is NOT gated on the server profile being empty — a partially-applied chunked upload must stay recoverable on the next login (ids + attribute_not_exists make replays no-ops); ids are assigned once and persisted to the anon blob so a retried migration reuses them, and the namespaced store is never overwritten with anon content. ProgressProvider's login reconciliation is gated on `authLoaded`; AuthContext settles `loaded` only from the CURRENT me() generation — a superseded me() flipping `loaded` early used to read as a logout and purge the queue on reload (clearLocalSession settles `loaded` itself).
- Feedback handler contract lives in `src/lib/feedback/http-handler.ts` — the Next route (dev/e2e) AND `lambda/feedback/index.ts` (prod) both delegate there. Phase E2: POST requires a session (401 `login_required`) and charges a durable monthly AI-mark quota (bucket `aimark:<userId>:<YYYY-MM>` in octav-rate-limits, TTL ~40d; 30 free / 1000 premium safety cap via `aiMarkQuotaForTier` in `src/lib/entitlements/features.ts`) checked BEFORE any LLM call (429 `{ error: "quota_exceeded", resetAt }`); GET adds `{ remaining, resetAt }` when a session resolves (anonymous GET keeps `{ configured }` only). Deps seam `src/lib/feedback/deps.ts` (`FEEDBACK_STORAGE=dummy|dynamodb` + shared clock; dummy = `InMemoryFeedbackStorage` extending the analytics dummy — the SHARED universe now spans auth→progress→analytics→feedback; fail-closed in Lambda via `AUTH_ALLOW_DUMMY=1` like the others). Test-mode injection keys: `_testResponse` (provider result) plus `_testAiMarkUsed`/`_testTier` (quota state/tier — honored only with `FEEDBACK_TEST_MODE=1` + dummy storage, the `_testCode` precedent). The feedback Lambda's terraform (modules/feedback_api) gained users GetItem + sessions Get/Update/Delete + rate-limits Get/Update and the base table-name env wiring (FEEDBACK_ENV secret now only carries provider config). Accounts handler contract lives in `src/lib/auth/http-handler.ts` — the Next routes under `src/app/api/auth/*` (dev/e2e) AND `lambda/auth/index.ts` (prod) both delegate there; deps are dummy-able (`src/lib/auth/dummy.ts`) and **fail closed in AWS Lambda** (`AUTH_ALLOW_DUMMY=1` required for dummy wiring when `AWS_LAMBDA_FUNCTION_NAME` is set); the email sender (SES or Resend) is selected via `EMAIL_PROVIDER`/`AUTH_EMAIL` in `src/lib/auth/deps.ts`. Rebuild ALL Lambdas with `npm run build:lambda` (esbuild → `lambda/*/dist/*.zip`, gitignored — the 8-function list lives in `scripts/build-lambdas.sh`), then let the CI deploy apply (no local applies — see Terraform bullet). Function URL public access needs BOTH `lambda:InvokeFunctionUrl` AND `lambda:InvokeFunction`+`InvokedViaFunctionUrl` (2026 AWS change). The pinned provider 6.x CAN express the second natively (`invoked_via_function_url` on `aws_lambda_permission` — verified against the 6.58.0 schema); the CLI provisioner is retained deliberately (matches deployed state; remove-then-add idempotent) and its `terraform_data` uses `triggers_replace = <lambda>.last_modified` so a Lambda replacement FORCES a re-run — `input` alone would not, create-time provisioners never fire on in-place updates. Migrating to the native attribute is a possible follow-up.
- Analytics handler contract lives in `src/lib/analytics/http-handler.ts` — the Next routes under `src/app/api/analytics/*` (dev/e2e) AND `lambda/analytics/index.ts` (prod, behind the CloudFront `/api/analytics/*` behavior, listed before `/api/*`) both delegate there; deps are dummy-able (`src/lib/analytics/dummy.ts`, sharing the auth/progress in-memory universe so a dummy-OTP session resolves for /summary) and **fail closed in AWS Lambda** (same `AUTH_ALLOW_DUMMY=1` guard). Ingest (`POST /event`) is PUBLIC + bounded per-IP by the durable fixed-window budget in `octav-rate-limits` (bucket `analytics:<ip>:<epoch>`); `GET /summary?days=7|30|90` is session-gated AND admin-allowlisted (`ANALYTICS_ADMIN_EMAILS` repo VARIABLE, comma-separated — 403 for non-admins); `GET /_health` is the unauthenticated Limit-1 CI smoke probe. `src/app/admin/analytics/page.tsx` is the static client dashboard (direct URL only, not in Nav; waits for `authLoaded`-equivalent `useAuth().loaded`). `ANALYTICS_ENV` optional secret overrides the Lambda env (unset = base wiring). Data: raw events TTL 90d, daily aggregates TTL 400d; the stored `host` comes from `X-Forwarded-Host`, preserved by the `api_host_header` viewer-request CloudFront Function on the `/api/analytics/*` behavior (the managed AllViewerExceptHostHeader origin request policy alone does NOT deliver the original viewer Host — verified on dev 2026-08-24, every event was attributed to the `*.lambda-url.*` origin domain), so the dev/prod traffic split stays real.
- Leaderboard (Phase D, D1–D8 done 2026-08-23; docs/leaderboard-plan.md): handler contract lives in `src/lib/leaderboard/http-handler.ts` — the Next routes under `src/app/api/leaderboard/*` (dev/e2e) AND `lambda/leaderboard/index.ts` (prod, behind the CloudFront `/api/leaderboard/*` behavior, listed before `/api/*`) both delegate there; deps seam `src/lib/leaderboard/deps.ts` (`LEADERBOARD_STORAGE=dummy|dynamodb`, fail-closed in Lambda via `AUTH_ALLOW_DUMMY=1`; dummy = the shared universe). Pure core in `src/lib/leaderboard/types.ts` (ISO week keys "2026-W35", scopes, rankBoard top-100 + neighbourhood, bucket keys) + `xp.ts` (10/quiz-correct +20 perfect, 20/exam-correct +40 perfect, 30×ladder-level, 2/newly-known flashcard; daily cap 500 in the `xpday:` bucket, diminishing repeats 1,1,0.5,0.5,0.5,0 in `xp-topic:` — both octav-rate-limits) + `handles.ts` (deterministic FNV-1a "{Adjective} {Animal}", browser-safe; also hosts `LEADERBOARD_HANDLE_RE`, shared by the auth schema and the account page). **Single-writer rule: XP is awarded ONLY inside the progress sync handler** (the D4 hook — accepted-writes-only so replays award zero, opted-in profiles only (`childProfile.leaderboardOptIn === true`), server-clock week attribution, award failures log + never fail the sync); the leaderboard Lambda is read-only (IAM: Query/GetItem), the auth Lambda owns erasure (Query on the user-index GSI + DeleteItem — opt-out flips AND removed-while-opted-in profiles erase rows immediately, failure tolerant). Weekly seasons: current week by server clock, prev week readable 7d via TTL (`expiresAt` = week end + 14d). Opt-in state lives on `childProfiles[i]` (`leaderboardOptIn`/`leaderboardHandle` — the account-update path MERGES them, `mergeLeaderboardFields` in auth/http-handler.ts; handle stored at opt-in = provided ?? stored ?? deterministic, changeable ONCE — a stored custom handle locks, kept on opt-out so rejoins keep identity but restart from 0 XP). Client: `/leaderboard` page (static client component; teaser when logged out via the PUBLIC `/api/leaderboard/teaser`, inline join when not opted in, podium + "Around you" neighbourhood + self footer; 60s refresh skipped while hidden) + the /account Leaderboard section. Analytics events (D8): `leaderboard_viewed` {scope, week} — fires once per (scope, weekKey) view incl. the logged-out teaser (ref-guarded, never on the 60s refresh) — and `leaderboard_membership_changed` {action: join|leave, scope=stageScope(profile.stage)} on successful join/leave from BOTH surfaces.
- Contact Us (Feature 3 of docs/supportability-features-plan.md, implemented 2026-08-25): handler contract in `src/lib/contact/http-handler.ts` — the Next routes `/api/contact` + `/api/contact/_health` (dev/e2e) AND `lambda/contact/index.ts` (prod, CloudFront `/api/contact` exact-path + `/api/contact/*` before `/api/*` — the bare path matters, the form POSTs there) both delegate there. Public POST {name, email, subject, message} → per-IP budget 3/hour (bucket `contact:<ip>:<epoch>` in octav-rate-limits, 429) → persist to `octav-contact` (PK messageId, TTL 365d, status "new", server-clock dates) → **persist-first, email best-effort**: Resend notification to `ANALYTICS_ADMIN_EMAILS`; a send failure still returns 200 (the message is durable + visible in /admin/dynamodb; a 5xx would invite retry-duplicates). Optional `resolveSession` sets userId — never 401. Deps seam `src/lib/contact/deps.ts` (`CONTACT_STORAGE=dummy|dynamodb`, fail-closed via `AUTH_ALLOW_DUMMY=1`; dynamodb mode REFUSES a non-Resend `EMAIL_PROVIDER` so a notification can never silently no-op — the contact `_health` smoke 500s if the repo secret is unset, deliberate); dummy = `InMemoryContactStorage` extends the leaderboard dummy — the SHARED universe now spans auth→progress→analytics→feedback→leaderboard→contact (constructed in `src/lib/progress/deps.ts`). `GET /api/contact/_health` = unauthenticated GetItem CI smoke probe. Client: `src/components/ContactButton.tsx` (floating bottom-left "?" + modal form, mounted in `src/app/layout.tsx`; in-modal `role="alert"` for 400 issues — NOT an error toast, so the draft survives; subject defaults to `question`); serve-static.ts maps `/api/contact`(+`/_health`); playwright.config wires `CONTACT_STORAGE=dummy`. CloudFlare Email Routing `info@octavlearning.com` verified live by the user 2026-08-28 (guide in the plan doc).
- Manual deploy (superseded by CI — kept as reference for the cache-split commands): `npm run build:static`, then sync with cache splits — `aws s3 sync out/ s3://iblearn-site-305655353474 --delete --exclude "_next/static/*" --exclude "sw.js" --exclude "manifest.webmanifest" --cache-control "public,max-age=300"`; `aws s3 sync out/_next/static s3://iblearn-site-305655353474/_next/static --delete --cache-control "public,max-age=31536000,immutable"`; `aws s3 cp out/sw.js …/sw.js --cache-control "no-cache"` (+ same for `manifest.webmanifest`); then `aws cloudfront create-invalidation --distribution-id E1BMVEW6YOKNUY --paths "/*"`.
- **Nothing impure may run during render** of a client component that gets prerendered (`Math.random()`, `Date.now()`, `typeof window` branches): every page is static-exported, so the server draw and the hydration draw must be byte-identical or React throws the tree away ("Hydration failed because the server rendered text didn't match the client" — it burned the mixed-review quiz in CI logs). Random per-session content therefore goes: deterministic seed in `useState` → `randomSeed()` reseed inside a `useEffect` → `key` on the child to swap the set (the pattern in `QuizPageClient.tsx` and `MixedReviewClient.tsx`; `randomSeed`/`seededShuffle`/`stratifiedSample(pool, targets, getDiff, seed)` in `src/lib/quiz-utils.ts`). `buildMixedReviewQuestions(progress, mode, seed)` takes the seed for that reason; omitting it keeps the old non-deterministic behaviour for non-SSR callers. Guard: `tests/unit/mixed-review-client.test.tsx` (first render must ask for the deterministic seed) and `tests/unit/mixed-review.test.ts` (a seeded draw never touches `Math.random`). Note `vi.stubGlobal('Math', { ...Math, random })` is a trap — Math's props are non-enumerable so the stub loses `abs`/`floor`; spy on `Math.random` instead.
- Static export (AWS pre-deploy): `npm run build:static` sets `BUILD_EXPORT=1` (`output: 'export'` in `next.config.mjs`; URLs stay extensionless — `page.html` files, mapped by a CloudFront Function in Session 2) and stashes `src/app/api/` aside for the build (export rejects the non-static route handler; it stays the dev/e2e path). `npm run test:e2e:static` runs the full suite + PWA spec against `out/` served by `scripts/serve-static.ts`, which mimics the CloudFront topology: static files like the S3 origin, `/api/feedback` delegated to the real route handler like the `/api/*` Lambda behavior.
- PWA (Phase 7): service worker is hand-rolled in `public/sw.js` (no workbox/serwist) — bump `CACHE_VERSION` only when the caching *strategy* changes, never per deploy. SW registration and the update toast are prod-gated (`NODE_ENV === 'production'`; unit tests use `vi.stubEnv`). SW e2e lives in `tests/e2e/pwa.spec.ts` and only runs under the prod-build pattern: `npm run build && E2E_PROD=1 npx playwright test tests/e2e/pwa.spec.ts --project='Desktop Chrome'`. Regenerate icons with `node scripts/generate-icons.mjs` after changing `public/icons/icon.svg`.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
